import SwiftUI

/// Main content view - uses RootView to handle onboarding flow
@available(iOS 15.0, *)
public struct ContentView: View {
    public var body: some View {
        RootView()
    }
    
    public init() {}
}
